using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WallJumping : MonoBehaviour {
    //GameManagerScript movement;

    // Use this for initialization
	void Start () {
        //movement = GetComponent<GameManagerScript>();
	}
	
	// Update is called once per frame
	void Update () {
		
        //if(Input.GetButtonDown(KeyCode.W) && movement.grounded)
        //{
        //
        //}
	}
}
